// Bootstrap arrives in P1-05 with the auth module. This placeholder keeps
// `tsc --noEmit` and `eslint .` happy while the api workspace is empty.
export const PLACEHOLDER = 'currency-exchange-api';
