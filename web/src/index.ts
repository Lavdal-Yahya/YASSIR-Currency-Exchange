// Vite entry arrives in P1-11 with the React shell. This placeholder keeps
// `tsc --noEmit` and `eslint .` happy while the web workspace is empty.
export const PLACEHOLDER = 'currency-exchange-web';
