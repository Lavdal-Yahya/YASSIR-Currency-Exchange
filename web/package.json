{
  "name": "@currency-exchange/web",
  "version": "0.0.0",
  "private": true,
  "description": "React + Vite PWA for the currency exchange bureau",
  "type": "module",
  "scripts": {
    "dev": "echo 'Vite dev server arrives in P1-11' && exit 0",
    "build": "echo 'web build arrives in P1-11' && exit 0",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
